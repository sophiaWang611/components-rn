<?xml version="1.0" encoding="utf-8"?>
<ListView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/lv"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ms_main_bg"
    android:divider="@color/ms_main_bg"
    android:dividerHeight="@dimen/form_row_vertical_interval"
    android:listSelector="@android:color/transparent"
    android:paddingBottom="@dimen/row_inner_vertical_padding"
    android:paddingLeft="@dimen/activity_vertical_padding"
    android:paddingRight="@dimen/activity_vertical_padding"
    android:paddingTop="@dimen/row_inner_vertical_padding" />